name: GH Action - List ML/DL
on: [push]
jobs:
  list-count-ml-dl:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
